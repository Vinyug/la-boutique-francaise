{% extends 'base.html.twig.html' %}

{% block content %}

    <h1>Insription</h1>

    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
        <hr>
    {% endif %}
    
    {{ form(form) }}

{% endblock %}
